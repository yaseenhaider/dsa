#include <iostream>   // only one pair of angle brackets
using namespace std;

int main() {          // main should return int, and needs parentheses
    cout << "hello world"; 
    return 0;         // good practice to return 0
}
write program in c++ .how to access specific value on array. how to insert value in array (insert before nad after). how we delete value i array 